"""
Command-line interface modules for the Renaissance Stock Ranking System.

This package contains the command-line interfaces for all main functionality:
    - main: CLI for the core ranking system
    - analyze: CLI for the sector analysis functionality
    - visualize: CLI for generating visualizations
    - extract: CLI for extracting data from Bloomberg

These modules provide user-friendly command-line interfaces that allow users to
interact with the system without having to write Python code. They handle argument
parsing, input validation, and error reporting to ensure a smooth user experience.

The CLIs exposed by these modules are also registered as entry points in setup.py,
making them available as standalone commands when the package is installed.
"""
